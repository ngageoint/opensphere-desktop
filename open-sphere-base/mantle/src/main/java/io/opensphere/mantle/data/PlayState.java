package io.opensphere.mantle.data;

/** State of video playback. */
public enum PlayState
{
    /** Playback forward. */
    FORWARD,

    /** Playback stopped. */
    STOP,

    /** Playback paused. */
    PAUSE,
}
