package io.opensphere.core.util.fx.tabpane;

/** An enumeration over the types of tab animations available. */
public enum OSTabAnimation
{
    /** The enum type for no animation. */
    NONE,

    /** The enum type for growing animation. */
    GROW;
}
