package io.opensphere.core.util.swing.pie;

/**
 * Change type.
 */
public enum ChangeType
{
    /** Mouse over cell changed. */
    MOUSE_OVER,

    /** Selection cell(s) changed. */
    SELECTION;
}
