package io.opensphere.core.util.fx.tabpane;

/** An enumeration over the states of animations for tab pane tabs. */
public enum OSTabAnimationState
{
    /** State of showing the tab animation. */
    SHOWING,

    /** State of hiding the tab animation. */
    HIDING,

    /** Default state of no animation. */
    NONE;
}
