/**
 * Classes that handle the high-level management of the graphics pipeline.
 * Implementations of
 * {@link io.opensphere.core.pipeline.processor.GeometryProcessor} prepare
 * geometries for rendering and implementations of
 * {@link io.opensphere.core.pipeline.renderer.GeometryRenderer} perform the
 * actual rendering.
 */
package io.opensphere.core.pipeline;
