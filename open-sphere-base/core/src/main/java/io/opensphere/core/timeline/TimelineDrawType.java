package io.opensphere.core.timeline;

/**
 * The enum representing the shape that should be drawn as on the chart.
 */
public enum TimelineDrawType
{
    /** Draw as dot (default). */
    LINE,

    /** Draw as rectangle(if span) and dot(if instant). */
    DEFAULT
}
