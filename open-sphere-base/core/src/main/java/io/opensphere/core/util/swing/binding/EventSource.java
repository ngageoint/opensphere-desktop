package io.opensphere.core.util.swing.binding;

/** Event source enumeration. */
enum EventSource
{
    /** Event is from the UI component. */
    UI,

    /** Event is from the property. */
    PROPERTY;
}
