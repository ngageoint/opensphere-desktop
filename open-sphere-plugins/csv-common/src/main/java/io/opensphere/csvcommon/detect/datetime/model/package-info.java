/**
 * Contains the classes that detect which columns are the date and/or time
 * columns. Also includes the classes that determine which format the date's and
 * time's are.
 */
package io.opensphere.csvcommon.detect.datetime.model;
