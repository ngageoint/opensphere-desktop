/**
 * Contains the classes that detect which columns are location columns. Also
 * includes the classes that determine which format the location data is in.
 */
package io.opensphere.csvcommon.detect.location.algorithm.decider;
