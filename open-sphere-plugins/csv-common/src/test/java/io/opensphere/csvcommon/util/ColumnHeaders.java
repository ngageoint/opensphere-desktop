package io.opensphere.csvcommon.util;

/**
 * Possible column headers for the CSV test utils.
 */
public enum ColumnHeaders
{
    /** The CEP. */
    CEP,

    /** The TIME. */
    TIME,

    /** The LAT. */
    LAT,

    /** The LON. */
    LON,

    /** The LATITUDE. */
    LATITUDE,

    /** The LONGITUDE. */
    LONGITUDE,

    /** The STATIONLAT. */
    STATIONLAT,

    /** The STATIONLON. */
    STATIONLON,

    /** The STATIONLAT 1. */
    STATIONLAT1,

    /** The STATIONLON 1. */
    STATIONLON1,

    /** The FLATTEN. */
    FLATTEN,

    /** The MGRS. */
    MGRS,

    /** The GRID. */
    GRID,

    /** The GEOM. */
    GEOM,

    /** The WKT geometry. */
    WKT_GEOMETRY,

    /** The POSITION. */
    POSITION,

    /** The BELONG. */
    BELONG,

    /** The LAT 1. */
    LAT_1,

    /** The LON 1. */
    LON_1,

    /** The SPOS. */
    SPOS,

    /** The BLOCK. */
    BLOCK,

    /** The NAME. */
    NAME,

    /** The NAME 2. */
    NAME2;
}
